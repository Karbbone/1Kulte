meta {
  name: Upload Reward Image
  type: http
  seq: 3
}

patch {
  url: {{host}}/rewards/{{rewardId}}/image
  body: multipartForm
  auth: none
}

headers {
  Authorization: Bearer {{jwt}}
}

body:multipart-form {
  image: @file(/absolute/path/to/reward-image.png)
}
